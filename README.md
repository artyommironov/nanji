# buildSrc

## Usage

```
git subtree add --prefix buildSrc https://github.com/kazufukurou/buildSrc.git master --squash
git subtree pull --prefix buildSrc https://github.com/kazufukurou/buildSrc.git master --squash
```
